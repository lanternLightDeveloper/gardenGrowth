name: Daily Weather Fetch

on:
  schedule:
    - cron: '0 05 * * *'
  workflow_dispatch: 

jobs:
  fetch-weather:
    runs-on: ubuntu-latest

    steps:
      - name: Call Weather Endpoint
        run: |
          curl -i --fail -X POST \
            -H "Authorization: Bearer ${{ secrets.CRON_SECRET }}" \
            https://gardengrowth.lanternlightdevelopment.com/api/fetch/weather
